import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.pokemontcg.io"], // add the Pokemon TCG image domain
  },
};

export default nextConfig;
